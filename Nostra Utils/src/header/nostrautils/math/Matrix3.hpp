#ifndef NOU_MATH_MATRIX3_HPP
#define NOU_MATH_MATRIX3_HPP

#include "nostrautils\core\StdIncludes.hpp"
#include "nostrautils\math\Vec3.hpp"

namespace NOU::NOU_MATH
{
/*	class NOU_CLASS Matrix3
	{
		public:
			int32 m_cols;
			Vec3i m_data[];

		public:
			Matrix3(int32 cols);
			Matrix3 add(const Matrix3 & matrix);
			Matrix3 sub(const Matrix3 & matrix);
			Matrix3 mult(const Matrix3 & matrix);
			Matrix3 div(const Matrix3 & matrix);
			int32 size();

			Matrix3 operator + (const Matrix3 & matrix);
	};*/
}

#endif

